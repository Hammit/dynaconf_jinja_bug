name: CI

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Django check
        run: python3 ./manage.py check --settings django42_dynaconf_jinja.development

      - name: Run Dynaconf list
        run: |
          DJANGO_SETTINGS_MODULE=django42_dynaconf_jinja.development dynaconf list -k DICT
          DJANGO_SETTINGS_MODULE=django42_dynaconf_jinja.development dynaconf list -k TEST
